name: Greeting
on: push
jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Greetings from Nilam and Spandan
              run: |
                echo "Hello, World!"
